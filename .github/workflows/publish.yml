name: Publish Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  publish:
    name: Publish to crates.io
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache dependencies
        uses: Swatinem/rust-cache@v2

      - name: Extract version from tag
        id: version
        run: |
          VERSION=${GITHUB_REF#refs/tags/v}
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Publishing version: $VERSION"

      - name: Publish derive crate
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
        run: cargo publish -p storekey-derive

      - name: Wait for storekey-derive to be available
        run: |
          VERSION="${{ steps.version.outputs.version }}"
          echo "Waiting for storekey-derive $VERSION on crates.io..."
          for i in {1..60}; do
            if cargo search storekey-derive --limit 1 | grep -q "storekey-derive = \"$VERSION\""; then
              echo "Found storekey-derive $VERSION"
              exit 0
            fi
            echo "Retry $i/60..."
            sleep 5
          done
          echo "Timed out waiting for crates.io"
          exit 1

      - name: Publish main crate
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
        run: cargo publish -p storekey

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: v${{ steps.version.outputs.version }}
          name: Release v${{ steps.version.outputs.version }}
          draft: false
          prerelease: false
          generate_release_notes: true

